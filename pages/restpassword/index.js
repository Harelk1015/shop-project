import React from 'react';
import RestPassword from '../../components/updateInfo/RestPassword';

function restpassword(props) {
    return (
        <RestPassword></RestPassword>
    )
}

export default restpassword;